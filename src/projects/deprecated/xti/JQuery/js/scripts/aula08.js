/**
 * 
 *  Author: Lucas Barretto
 *  Description: Estudo sobre seletores jQuery, Seletores de Atributos
 * 
 * 
 */

$(document).ready(function () {
    $("span").click(function() {
        // alert($("img").attr("title")); //Seleção de attributo normal
        // $("img").attr("src", "img/SonSon.png"); // .attr("attributo" "novo valor") função attr usada para a manipulaçao de valores
        // $("img").attr({src:"img/SonSon.png", title: "Son Son"}); // Uso do attr para manipular recebendo um JSON  
        // $("img").removeAttr("title"); // Remove atributo da tag
        // alert($("img").hasClass("test")); // retorna boolean caso uma classe exista ou não
        // $("img").addClass("image"); // Adiciona uma class CSS a tag, podendo receber como o param o proprio CSS
        // $("img").removeClass("test"); // Remove uma deteminada class CSS da tag
        // $("img").toggleClass("test-2"); // Adiciona ou remove uma determinada class CSS da tag
        // alert($("img").val()); // Retorna o valor do atribuito e value de um input, mas também permite a modificação
        
    });
});